
// import './MyButton.scss'

// export default function MyButton (props) {
//     if (props.isButton == 'true') {
//         return <a className={"btn  btn--" + props.variant}  href={props.url}>{props.text}</a>
//     } else {
//         return <a className={"link  link--" + props.variant} href={props.url}>{props.text}</a>
//     }
// }
